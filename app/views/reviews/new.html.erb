<%= simple_form_for [ @restaurant, @review] do |f| %>
  <%= f.input :rating, collection: ["0", "1", "2", "3", "4", "5"]%>
  <%= f.input :content %>
  <%= f.button :submit %>
<% end %>

<%= link_to "FICHE DU RESTO", restaurant_path(@restaurant)%>
